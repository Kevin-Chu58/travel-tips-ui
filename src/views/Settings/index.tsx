import { Container } from "@mui/material";

const Settings = () => {
  return (
    <Container className="settings-view" maxWidth={false} disableGutters>
      Settings
    </Container>
  );
};

export default Settings;
